# Beer Delivery Touch

Mobile-friendly version of Beer Delivery.

Open `index.html` in a browser. On phones, use the on-screen buttons:

- `▲` move up
- `▼` move down
- `BEER` deliver beer
- `R` restart

